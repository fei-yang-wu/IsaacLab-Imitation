# Frozen pin for the deterministic-latent end-to-end chain (2026-07-30).
# The base pipeline is imitation_experiments/pipeline/conf/latent_e2e.yaml;
# this file records only what this campaign fixes.

defaults:
  - latent_e2e
  - _self_

name: det_latent_e2e_lafan1
seed: 0

pretrain:
  overrides:
    - latent.mode=deterministic

low_level:
  overrides:
    - latent_arm=deterministic

# Frozen at dry-run scale until the protocol is approved; flip dry_run on the
# command line only after reviewing the rendered plan.
dry_run: true
