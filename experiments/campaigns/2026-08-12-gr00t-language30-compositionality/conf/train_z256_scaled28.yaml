defaults:
  - base_train
  - _self_

# Matched twin of train_fsq64_scaled28: same rows, same motions, same budget,
# same `state_dropout` 0. Only the latent interface differs.
output_dir: outputs/gr00t_language30/arms/z256_scaled28
target: latent
data:
  table: outputs/gr00t_language30/tables/z256_scaled28_table.pt
  state_field: oracle_rollout_state_history
goal_features: outputs/gr00t_language30/goal_features/goal_features.pt
pretrained_bundle: outputs/gr00t_language10/goal_features/action_head_trunk.pt
model:
  state_dropout_prob: 0
run:
  checkpoint_interval: 12000
wandb:
  name: l28_z256_scaled
  tags: [gr00t-head, language28, compositionality, z256, target-latent, data-scaled]
  group: gr00t-language30-compositionality
